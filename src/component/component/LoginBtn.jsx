import login from "./Login"
export default function LoginBtn() {
  return (
    <div>
        <form className="d-flex">
        <button
          className="btn btn-primary "type="button" > Login</button>
        </form>
    </div>
  )
}
